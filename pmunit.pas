unit PMunit;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;

implementation

begin
  Function PerMult(a) :
  var contador: int;
    contador = 0;
    while(a>10) DO
      begin
      contador = contador + 1;
      Len := Length(Str);
      Setlength(arr, Len);
      Arr: array of Char;
      Arr2: array of Char;
      var multiply: int;
      multiply = 1;
      for i:= 1 to Len do
          Arr[i-1] := Arr2[i];
          multiply = mutiply * Arr2[i];


      end;

contador;

end.

